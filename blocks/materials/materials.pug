mixin materials(items)
	+b.SECTION.materials(class="wow pulse", data-wow-offset="150")
		+e.DIV.wrap
			+e.H2.title(class="titles --landing") Работаем с лучшими материалами
			+e.UL.list
				each item in items
					+e.item
						+e.IMG.img(src=item.src, alt=item.alt)
